{{#extend "layout"}}
  {{#content "content"}}
    <section class="container">
        <h1>Blocks</h1>
      </section>

      {{#each blocks}}
        {{> (get_partial this.type) block=this}}
      {{/each}}
  {{/content}}
{{/extend}}
